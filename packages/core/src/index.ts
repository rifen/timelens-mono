export { detectDuration } from './detection';
export { toMilliseconds, formatDuration, formatDurationFull, evaluateExpression } from './formatting';
export {
  TimeLensSettings,
  DetectedDuration,
  FormatOptions,
  DEFAULT_SETTINGS
} from './types';